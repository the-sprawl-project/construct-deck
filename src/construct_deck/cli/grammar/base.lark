?start: statement

%import .ping (ping_stmt)
%import .create (create_stmt)

?statement: ping_stmt | create_stmt

// imports go below
%import common.CNAME
%import common.ESCAPED_STRING
%import common.WS
%ignore WS
